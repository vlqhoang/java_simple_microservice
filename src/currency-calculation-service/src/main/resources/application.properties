# setting up application name and port
spring.application.name=currency-calculation-service
server.port=8100

# hardcoding service's urls for ribbon to distribute load balancing => bad design
# currency-exchange-service.ribbon.listOfServers=http://localhost:8000,http://localhost:8001

# configure eureka naming server address to register this application to eureka server
eureka.client.service-url.default-zone=http://localhost:8761/eureka
